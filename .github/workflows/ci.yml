name: CI

# Runs typecheck, build, and the test suite on every push to main and every
# pull request — closes the gap where npm test previously only ran in the
# tag-triggered release job.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  ci:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node-version: [20, 22, 24]
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      # Runs after Build: test/library-exports.test.ts imports the package
      # by its own name (`@thomas-powers-jr/necro`, phase 55's self-reference
      # check), which only resolves once `dist/` exists.
      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm run test:coverage
