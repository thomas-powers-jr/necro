name: necro scan

# Dogfoods the composite necro Action: runs necro, uploads SARIF to
# code-scanning. The Action pulls the *published* CLI (npx @thomas-powers-jr/necro),
# so this only works once a release carrying --sarif/--fail-on (>= 1.1.0) is on
# npm. Triggered on PRs and manually (not push) to avoid firing against an older
# published CLI; validate with `workflow_dispatch` after the 1.1.0 release.
on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write # required for upload-sarif

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      # necro.config.json seeds entries (bin + bench scripts) so the src/-only
      # self-scan isn't the degenerate 0-entry case, and src/.necro-baseline.json
      # snapshots pre-existing findings so fail-on only gates new regressions.
      - name: necro
        uses: ./.github/actions/necro
        with:
          path: src
          fail-on: high
