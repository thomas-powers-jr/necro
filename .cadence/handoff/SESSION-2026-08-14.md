---
cadence_handoff: 1
generated_at: 2026-08-14T21:09:50.458Z
label: 
loop_position: IDLE
active_phase: 70-literal-dispatch-table-taint
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: c179524
git_ahead: 6
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-08-14

## TL;DR for the next session
- Phase 70 shipped and committed: fixed a false-positive taint on same-file dict-literal dispatch tables (`handler_map[action](...)`) in `findTaintedFiles`, confirmed on 3 real pip corpus files. `rec-20260814-001` promoted to `shipped`.
- Loop is back to IDLE, no active draft, working tree clean.
- `rec-20260719-004` (taint direction inversion) is still stuck `settle-pending` — the recommendation-promote tool only allows `settle-pending → shipped`, which would be a false ledger entry (phase 65 reverted, no code shipped, and a same-session follow-up investigation into it also found no deterministic win). User explicitly chose to **leave it wedged for now**, not file an upstream cadence-core issue yet — that's still an open decision.
- The recommendation ledger has nothing else queued (`cadence recommend` returns 0 ranked / 0 parked) — the next session starts from empty, not from a pre-picked task.
- Also did unrelated repo hygiene this session: renamed a stale, already-ingested handoff doc aside and gitignored it plus `.cadence/mcp-trust.json` (local trust-grant state).

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 6 ahead / 0 behind origin
- HEAD `c179524`
- Recent commits:
```
c179524 chore: gitignore local MCP trust grants and consumed handoff docs
27dccd4 feat(70): stop false-tainting same-file dict-literal dispatch tables (rec-20260814-001)
32db82f chore(cadence): stamp state after rec-20260719-004 investigation
3eaa42a chore(cadence): stamp state after recommendation ledger reconciliation
87f4194 feat(69): sync README/website/design-spec docs to v1.6.0 (rec-20260701-004 follow-up)
e713037 chore(cadence): stamp session handoff — 2026-07-21 (v1.6.0-release)
0877df7 chore(release): v1.6.0
7059d94 feat(68): import-resolved I/O denylist for initializer side-effect screen
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 4 ++--
 2 files changed, 3 insertions(+), 3 deletions(-)
```
- Loop: IDLE · phase 70-literal-dispatch-table-taint · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - (none)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- Ran `cadence progress`/`cadence doctor`; found `rec-20260719-004` wedged `settle-pending` with no honest exit (tool only allows `→shipped`) — confirmed via advisor and by actually attempting `promote --status=deferred` (rejected by the tool itself, not just theorized).
- User decided: leave that rec wedged, start phase 70 on a different, newly-surfaced over-tainting bug instead.
- Filed `rec-20260814-001`: `findTaintedFiles`'s bracket-call heuristic (`obj[key](`) false-tainted files with a same-file dict-literal dispatch table, capping every symbol in the file (not just exports) at `maybe`. Verified against the real python-realrepo corpus (`pip/_internal/commands/{index,cache,configuration}.py`).
- Drafted, approved, and built phase `70-literal-dispatch-table-taint` (70-01) — TDD throughout, 2 advisor consults mid-build caught real issues before they shipped (see Carry-forward gotchas).
- Fix: `src/analyze/reachability.ts` now resolves same-file literal-dict bindings (`ident = {...}` and Python's `ident = self.ident()` / `def ident(self): return {...}` pair) before counting a bracket-call match as taint. Guards against empty-then-mutated dicts and comprehensions. Python-only (no JS/TS corpus evidence).
- Added `test/reachability-taint-fix-accuracy-gate.test.ts` (phase-70-owned AC-6 evidence — the generic `cadence_verify_coverage` check initially reported a false pass via token collision with unrelated phases' pre-existing `(AC-6)` test titles).
- Settled phase 70 (6/6 ACs pass), promoted `rec-20260814-001` to `shipped --ref "phase 70..."`, committed as `27dccd4`.
- Housekeeping (separate commit `c179524`): renamed the already-consumed `docs/necro-reaudit-recommendations-handoff.md` to `.consumed-2026-08-13`, gitignored it (`*.consumed-*` pattern, generalizing the existing `HANDOFF.md.resumed-*` rule) and `.cadence/mcp-trust.json`.

## Carry-forward gotchas
- `cadence_recommendation_promote`'s MCP tool schema is missing `shipped` from its `status` enum even though the CLI (`cadence recommendation promote <id> --status=shipped --ref "..."`) supports it — use the CLI directly for shipped promotions, not the MCP tool.
- Trust grants (`cadence mcp trust grant --tool <name>`) are version-bound; a CADENCE npm-version bump (this session: 1.47.0 → 1.54.0) invalidates existing grants with a clear `version mismatch` error — just re-grant, it's a one-line fix, not a real blocker.
- `cadence_draft_approve` gates on `DRAFT.md` mtime vs. its recorded `draftReadAt` — editing an approved DRAFT mid-build (legitimate: I corrected AC-5's wording after discovering the phase-45 Python `likely`-cap made the original "reaches `certain`" claim false) requires re-running `draft_check` + `draft_approve` before `settle` will accept it. `--allow-stale-draft` exists to bypass this but I didn't use it — re-approving is the honest path when the edit is real.
- `cadence_verify_coverage`'s AC-id matching is a repo-wide token search across all `*.test.ts` files, not phase-scoped — it will report `satisfied: true` for a brand-new AC id if an unrelated older phase happens to already use that same AC number in a test title elsewhere (happened here: phase-70's AC-6 collided with phase-45's and phase-49's own AC-6). Don't trust a bare `satisfied: true` without checking which files actually matched.
- Python dead-code findings are hard-capped at `likely`, never `certain`/auto-fix-eligible, regardless of taint (`classify.ts:94-101`, AC-6/phase 45) — don't assume "taint cleared" implies "reaches certain" for a `.py` file.
- Open decision, explicitly deferred by the user, not by default/oversight: whether to file an upstream `cadence-core` issue (repo: `/home/thomas/projects/cadence`, which the user authors) for the `settle-pending → shipped`-only state-machine gap that wedged `rec-20260719-004`.

## Next action
**Action:** The ledger has nothing queued (`cadence recommend` → 0 ranked/0 parked), so start by deciding `rec-20260719-004`'s disposition (file the upstream cadence-core issue, or explicitly leave it wedged again) and/or run a `cadence-scout` or brainstorming pass to surface the next recommendation before drafting phase 71. Don't jump straight to `cadence draft new` without a rec backing it — this session's DRAFT/advisor review process worked well specifically because the draft was evidence-backed from the start.
**Verify:** `cadence recommend` shows a ranked or parked entry (or the user has explicitly picked a direction) before scaffolding a new draft.
**If it fails:** if no new recommendation surfaces and the user doesn't want to resolve `rec-20260719-004`'s wedge, ask them directly what phase 71 should target rather than guessing from the ledger.
