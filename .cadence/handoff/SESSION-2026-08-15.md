---
cadence_handoff: 1
generated_at: 2026-08-15T13:47:41.555Z
label: 
loop_position: IDLE
active_phase: 72-php-syntactic-axis
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: fd25070
git_ahead: 2
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-08-15

## TL;DR for the next session
- Phase 72-01 shipped and committed (`4a539a1`): PHP's syntactic axis (complexity/duplication/hotspots) — gated behind explicit config, `.php` is NOT yet in `DEFAULT_CONFIG.include`. Backed by `.cadence/intelligence/php-support-design.md` (new this session, gitignored — `.cadence/intelligence/` is not tracked), a from-scratch design doc mirroring `python-support-design.md`'s structure, verified via a live `tree-sitter-php.wasm` grammar probe and direct reads of the post-Python-ship codebase.
- `rec-20260814-003` (PHP support, was `candidate`/`raw-idea`) is now `settle-pending`, `convertedToPhaseId: 72-php-syntactic-axis` — same lifecycle rec-20260814-002 went through for Python's phase 71.
- A real crash surfaced by manual spot-check (not by any automated test) got fixed along the way: `.php` files were reaching ts-morph's reachability graph unfiltered and crashing it. Fixed narrowly (`src/graph/php/language.ts` + `model.ts` partition) — PHP still contributes zero graph nodes, matching Phase A's report-only-for-syntax scope. See gotchas below.
- Two research forks this session (grammar probing, architecture mapping) both returned fabricated reports — one echoed my own prior chat text back as "findings," the other claimed a probe script ran that was never written to disk. Both discarded; everything was redone directly (a live grammar probe I wrote and ran myself, plus direct `grep`/`Read` of the codebase). Worth remembering if forking again this project: verify fork output before trusting it, don't assume completion = correctness.
- Next up: Phase B of the design doc — the composer.json PSR-4/PSR-0/classmap/files autoload resolver (`src/graph/php/`, likely phase `73-*`). No new rec needed; draft directly off `php-support-design.md` §4 Phase B, same pattern this session used for Phase A.
- Loop is IDLE, working tree clean except the routine `.cadence/STATE.md`/`state.json` diff.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 2 ahead / 0 behind origin
- HEAD `fd25070`
- Recent commits:
```
fd25070 docs: sync README/website with shipped v1.7.0 state
4a539a1 feat(72): PHP syntactic axis — complexity/duplication/hotspots (rec-20260814-003)
909d2d2 chore(release): create a GitHub Release on every npm publish
4f0c5f2 feat: rename npm scope to @thomas-powers-jr and release v1.7.0
fcfd9e0 chore(cadence): stamp session handoff — 2026-08-15
1e621ed feat(71): flip Python to default-on now that the accuracy floor is cleared (rec-20260814-002)
0054059 chore(cadence): stamp session handoff — 2026-08-14
c179524 chore: gitignore local MCP trust grants and consumed handoff docs
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 4 ++--
 2 files changed, 3 insertions(+), 3 deletions(-)
```
- Loop: IDLE · phase 72-php-syntactic-axis · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260814-005 — Security review of the LLM/auto-fix pipeline (candidate/raw-idea)
  - rec-20260814-008 — Lift the Python certain-tier cap (classify.ts AC-6) with tier-stratified validation (candidate/needs-evidence)
  - rec-20260814-004 — Cross-language and fuzzy (Type-3) clone detection (candidate/raw-idea)
  - rec-20260814-006 — test-only auto-apply and cascading re-analysis after fix (candidate/raw-idea)
  - rec-20260814-007 — Recency-weighted churn and ownership scoring for hotspots (candidate/raw-idea)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- Resumed from the prior session's handoff (phase 71, Python default-on, already shipped).
- User chose `rec-20260814-003` (PHP support) for phase 72, and a design-pass-first approach given its `raw-idea` readiness.
- Wrote `.cadence/intelligence/php-support-design.md` — full PHP design doc (executive read, reference-resolution strategy, port-for-free vs. new-work breakdown, corpus plan, 5-phase plan A–E, 7 open questions, verified-fact appendix, risks), backed by a live grammar probe against `tree-sitter-php.wasm` and direct codebase reads (discarding two forks' fabricated reports first).
- Advisor review of the design doc caught 3 issues, all fixed: a copy-paste artifact in §1, an unverified PHPUnit `--coverage-cobertura` claim (verified via `WebSearch` instead of left as a guess), and §5.7's architecture question sharpened with its actual regression-test cost.
- Investigated a 27-file `@manehorizons`→`@thomas-powers-jr` rename diff that appeared mid-session; traced it to a legitimate external commit from another concurrent session (`4f0c5f2`), not fork damage — left untouched, did not commit it.
- Drafted, approved, and built phase `72-php-syntactic-axis` / unit `72-01` (Phase A of the design doc): `.php` grammar dispatch (`parse.ts`), `FUNCTION_KINDS`/`categoryOf` PHP arms incl. a resolved match-arm cyclomatic-complexity decision (`ir.ts`), PHP token normalization incl. a correction to the DRAFT's original plan for `null` (`tokens.ts`), `vendor/` added to `SKIP_DIRS` (`discover.ts`) — all TDD, red before green throughout.
- Found and fixed a real crash via manual spot-check (`.php` files reaching ts-morph's reference resolver) — `src/graph/php/language.ts` (new) + a narrow `model.ts` partition fix, with a reproducing regression test.
- CADENCE's deep-verify caught 2 test-quality gaps (a boolean-operator test whose `>=5` threshold passed without word-form operators actually working; a `$`-sigil test checking presence, not exact count) — both strengthened and re-verified.
- Settled cleanly: 7/7 ACs independently AI-verified, full suite 844 passed / 6 skipped / 0 failed, typecheck + lint clean.
- Committed as `4a539a1` (user explicitly approved the commit).

## Carry-forward gotchas
- `.php` is deliberately NOT in `DEFAULT_CONFIG.include` yet (`src/config.ts:83-92`) — PHP stays config-gated until a future corpus-backed phase flips it, mirroring exactly how Python was gated through phases A–D and only flipped default-on in phase 71. Don't add it prematurely.
- `src/engine/model.ts:132-146`'s `tsFiles` filter now excludes BOTH `.py` (`isPythonFile`) and `.php` (`isPhpFile`, new this session, `src/graph/php/language.ts`). If Phase B/C adds a real PHP symbol graph, this exclusion needs revisiting — right now PHP files contribute zero graph nodes by design (Phase A's report-only-for-syntax scope), not because a real graph doesn't exist yet vs. "we haven't wired the merge in."
- The crash this session's `model.ts` fix addresses is real and was NOT anticipated in the original DRAFT (which said "DO NOT touch `src/engine/model.ts`") — a one-line-shaped exception, documented inline in both the DRAFT (corrected post-hoc) and the SUMMARY. `scan()` always builds the reachability model first regardless of the complexity-axis flag (`src/engine/index.ts:56`) — there's no way to exercise the syntactic axis end-to-end via the real CLI without the graph builder tolerating (or excluding) every discovered language, so this same shape of fix will likely recur for any future language's own Phase A.
- The php-support-design.md's own §5.7 open question — whether to extend the now-**two-way-copy-pasted-once** (Python was the first copy) TS/PHP/Python file partition in `model.ts` a third time when Phase C lands, or generalize to a `LanguageGraphPlugin` interface — is now more concretely motivated: this session already added a *second* copy-paste of the "exclude this language's files from ts-morph" pattern (`isPythonFile`/`isPhpFile` both checked in the same line). A third language would make it three near-identical checks in one boolean expression.
- `cadence_recommendation_convert`/`draft_new --fromRec` moved `rec-20260814-003` to `settle-pending` on conversion — same lifecycle as `rec-20260814-002`. A future phase continuing the PHP work (Phase B onward) does NOT need a new rec; draft directly off `php-support-design.md`, same as this session did for Phase A, and reference the design doc (not a rec id) in the DRAFT's `## Recommendation` section.
- `.cadence/intelligence/` is gitignored — `php-support-design.md` and `python-support-design.md` both live there, untracked by git, durable only on this machine (or wherever this repo clone lives). Not something to "sync" — matches existing precedent, not a bug.
- Multiple other Claude Code sessions are actively working in this same repo concurrently (`mane-horizons-cloud-*` peers, visible via `ListAgents`) — the npm-scope rename (`4f0c5f2`) and a docs-sync commit (`fd25070`, visible in this doc's git log above) both landed from outside this session without any action needed here. If you see unexpected working-tree changes again, check `git log`/mtimes/`_managedBy` tags before assuming anything is wrong, same as this session did.

## Next action
**Action:** Draft the next PHP phase (likely `73-php-composer-resolver` or similar) scoping Phase B of `.cadence/intelligence/php-support-design.md` §4 — the composer.json PSR-4/PSR-0/classmap/files autoload parser (new module family under `src/graph/php/`, alongside the `language.ts` helper this session added). Follow the same pattern phase 72-01 used: `cadence draft new <phase> 01 --title="..."` (no `--fromRec`, since `rec-20260814-003` is already `settle-pending`/converted — reference the design doc directly in the DRAFT's `## Recommendation` section instead), write ACs/tasks citing exact current file:line locations (re-verify them fresh, don't trust this doc's line numbers to still be current), get user sign-off on the DRAFT before approving, then TDD through BUILD exactly like this session did (red before every green; re-run the full suite + typecheck + lint before each settle attempt; expect CADENCE's deep-verify to catch real gaps — address them by fixing the DRAFT/tests, not by forcing past the gate).
**Verify:** `cadence settle run --auto --deep` returns all ACs passing, full suite green, loop back to IDLE.
**If it fails:** if deep-verify flags a DRAFT/implementation mismatch, check whether the DRAFT's plan or the implementation is actually correct (this session hit two real cases where the implementation was right and the DRAFT text was stale) — fix whichever is wrong, don't just force past the gate. If a design decision from php-support-design.md §5 needs resolving before Phase B can be scoped cleanly, ask the user rather than guessing (this session did this for the framework-DI-scoping and match-arm-counting questions).
