---
cadence_handoff: 1
generated_at: 2026-08-15T00:49:36.770Z
label: 
loop_position: IDLE
active_phase: 71-python-default-on
active_draft: 
tier: 
git_branch: main
git_dirty: true
git_head: 1e621ed
git_ahead: 8
git_behind: 0
context_packet: .cadence/intelligence/context/handoff.json
---

# Session Handoff — 2026-08-15

## TL;DR for the next session
- Phase 71 shipped and committed (`1e621ed`): flipped Python to default-on (`DEFAULT_CONFIG.include` now has `**/*.py`), fixed a `build/`-skip regression this exposed, proved a mixed TS+Python repo scans correctly under the bare default, corrected stale "not corpus-validated" doc claims, and guarded necro's own repo-root config against its 248 vendored Python fixture files. `rec-20260814-002` converted to this phase (now correctly excluded from the ranked list).
- Loop is back to IDLE, no active draft, working tree clean except the routine `.cadence/STATE.md`/`state.json` diff.
- Filed an upstream `cadence-core` issue ([thomas-powers-jr/cadence#423](https://github.com/thomas-powers-jr/cadence/issues/423)) about `rec-20260719-004`'s wedge (settle-pending's only sanctioned exit is `→ shipped`, with no honest way to record "investigated, no win"). Not resolved — still wedged, still excluded from ranking.
- Ran an open-ended `cadence-scout` pass (`scout-20260814-2138`) that landed 6 new ranked recs; one (`rec-20260814-002`) became phase 71, the other 5 remain candidates, plus a 6th (`rec-20260814-008`) split off during phase 71 drafting. `cadence recommend` currently ranks `rec-20260814-005` (security review of the LLM/auto-fix pipeline) top at score 58.
- Next session starts from a stocked ledger, not empty — see `## Next action`.

## State on handoff   ·  pre-filled — verify, don't retype
- Branch `main` (dirty), 8 ahead / 0 behind origin
- HEAD `1e621ed`
- Recent commits:
```
1e621ed feat(71): flip Python to default-on now that the accuracy floor is cleared (rec-20260814-002)
0054059 chore(cadence): stamp session handoff — 2026-08-14
c179524 chore: gitignore local MCP trust grants and consumed handoff docs
27dccd4 feat(70): stop false-tainting same-file dict-literal dispatch tables (rec-20260814-001)
32db82f chore(cadence): stamp state after rec-20260719-004 investigation
3eaa42a chore(cadence): stamp state after recommendation ledger reconciliation
87f4194 feat(69): sync README/website/design-spec docs to v1.6.0 (rec-20260701-004 follow-up)
e713037 chore(cadence): stamp session handoff — 2026-07-21 (v1.6.0-release)
```
- Uncommitted (diff --stat):
```
.cadence/STATE.md   | 2 +-
 .cadence/state.json | 4 ++--
 2 files changed, 3 insertions(+), 3 deletions(-)
```
- Loop: IDLE · phase 71-python-default-on · tier (none)

## CADENCE context   ·  pre-filled from `cadence context handoff`
- Top recommendations:
  - rec-20260814-005 — Security review of the LLM/auto-fix pipeline (candidate/raw-idea)
  - rec-20260814-008 — Lift the Python certain-tier cap (classify.ts AC-6) with tier-stratified validation (candidate/needs-evidence)
  - rec-20260814-003 — Kick off PHP language support (composer PSR-4 autoload) (candidate/raw-idea)
  - rec-20260814-004 — Cross-language and fuzzy (Type-3) clone detection (candidate/raw-idea)
  - rec-20260814-006 — test-only auto-apply and cascading re-analysis after fix (candidate/raw-idea)
- Open assumptions:
  - (none)
- Active decisions:
  - (none)
- Files in play:
  - (none)

## What landed this session
- Resumed cleanly from the prior `SESSION-2026-08-14.md` handoff (no drift, remote already verified).
- Filed `cadence` (upstream) issue #423 for the `rec-20260719-004` settle-pending wedge.
- Ran `/cadence-scout` (open-ended, whole-product) → landed `rec-20260814-002` through `-007`.
- Drafted, built, and settled phase `71-python-default-on` (unit `71-01`) off `rec-20260814-002`, with 3 rounds of advisor review before approval and 1 more before settling — advisor caught a `build/`-skip regression, a vendored-fixture self-scan leak, an AC-5 test gap, and an AC-id-tagging gap, all fixed before shipping.
- Split `rec-20260814-008` (lifting the Python `certain`-tier cap) out of `rec-20260814-002`'s original scope so it doesn't silently vanish from the ledger.
- Committed as `1e621ed`.

## Carry-forward gotchas
- `cadence_verify_coverage`'s AC-id matching is a repo-wide token search across every `*.test.ts` file (448 files this session, including vendored `.bench-cache/` fixtures) — a bare `satisfied: true` proves nothing. This project's AC numbering restarts at 1 every phase, so every id 1–9 already collides with some unrelated pre-existing test; the only reliable check is confirming which specific file/test matched, by hand. The MCP tool's raw result can exceed 130K characters — it gets written to a `tool-results/*.txt` file; `jq` that file (probe with `.files[] | select((.occurrences|length)>0)`) rather than reading it raw.
- Python's `certain`-tier cap (`classify.ts:94-101`, phase 45) is deliberately still in place after phase 71 — only *discovery* default-on shipped. `rec-20260814-008` is the tracked follow-up; it needs tier-stratified precision (specifically among symbols that would earn `certain` under `deadTier()`), not just the verdict-level 0.90/0.69 numbers phase 71 already cites.
- Phase 71's T6 (`necro.config.json` `ignore` now excludes `test/fixtures/**`) incidentally also closed a pre-existing TS/JS fixture-visibility gap that predates the Python work (that directory always had `.ts`/`.js` fixtures too, walkable under the old default). The shipped test itself is correctly scoped to `.py` only, but don't let the SUMMARY's framing be read as "Python caused this" — it didn't, it just exposed it.
- `rec-20260719-004` is still wedged `settle-pending`, still excluded from `cadence recommend`'s ranking. Issue #423 is filed upstream but unresolved — no `cadence-core` code has changed. Don't expect `recommendation promote` to work on it yet.
- The `cadence_draft_approve`/`cadence_settle` MCP tools' trust grants held for this entire session with no re-grant needed — if a future session hits a "no valid trust grant" refusal, that's the CADENCE-version-bump invalidation from an earlier memory, not a new problem; re-grant via the CLI on a real terminal.

## Next action
**Action:** `cadence recommend` currently ranks `rec-20260814-005` (Security review of the LLM/auto-fix pipeline, score 58) top, ahead of `rec-20260814-008` (cap-lift, needs-evidence, score 55), `rec-20260814-003` (PHP, score 53), `rec-20260814-004` (Type-3 clones, score 53), `rec-20260814-006` (test-only auto-apply, score 53), and `rec-20260814-007` (churn scoring, score 50). Ask the user which to draft phase 72 off (default to the top-ranked security-review rec if they have no preference) — don't scaffold a draft without a rec backing it, matching how phase 71 went.
**Verify:** the user has explicitly picked a rec id (or confirmed the top-ranked one) before running `cadence draft new`.
**If it fails:** if the user wants something not in this ranked list, run another `/cadence-scout` pass scoped to their stated area instead of guessing.
